# Pinned so Read the Docs builds are reproducible; bump deliberately and
# rebuild locally with:  make -C docs html SPHINXOPTS=-W
sphinx==9.0.4
myst-parser==5.1.0
sphinx-rtd-theme==3.1.0
sphinx-sitemap==2.9.0
